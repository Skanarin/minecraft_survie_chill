# minecraft_survie_chill
Serveur modpack chill
Regarde une modif

# PULL AUTO
> Le start.sh pull automatiquementy pour mettre a jour le repo avant de lancer le serveur

## Utilisation WebHook Discord
- Creer un fichier '.env'
- Ajouter `DISCORD_WEBHOOK="Le WebHook"` le demander a un admin du serveur

>Attention !! Ne pas push le .env, (ignoree avec le gitignore normalement)
